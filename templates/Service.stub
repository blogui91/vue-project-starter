import Service from 'easy-requests'
import { API_URL } from 'config/env'
import { EventDispatcher } from 'src/event'

class {model} extends Service {
  constructor () {
    super()
    this.config.origin = API_URL
    //this.config.endpoint = '/{model}/'
  }
}
export default {model}
